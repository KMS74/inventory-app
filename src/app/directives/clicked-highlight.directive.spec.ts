import { ClickedHighlightDirective } from './clicked-highlight.directive';

describe('ClickedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
